/**////////////////////////////////////////////////////////////////////////////////////////// */ // Copyright 2026 Garmin International, Inc.
// Licensed under the Flexible and Interoperable Data Transfer (FIT) Protocol License; you
// may not use this file except in compliance with the Flexible and Interoperable Data
// Transfer (FIT) Protocol License.
/**////////////////////////////////////////////////////////////////////////////////////////// */ // ****WARNING****  This file is auto-generated!  Do NOT edit this file.
// Profile Version = 21.200.0Release
// Tag = production/release/21.200.0-0-g28b5705d
/**////////////////////////////////////////////////////////////////////////////////////////// */
package com.garmin.fit.csv

import java.io.ByteArrayOutputStream
import java.io.IOException
import java.io.OutputStream
import java.io.UnsupportedEncodingException

internal class PrefixableByteArrayOutputStream : ByteArrayOutputStream,
    ByteArrayOutputStreamPrefixable {
    private val prefixStream: ByteArrayOutputStream

    constructor() : super() {
        this.prefixStream = ByteArrayOutputStream()
    }

    constructor(size: Int) : super(size) {
        this.prefixStream = ByteArrayOutputStream()
    }

    @Throws(IOException::class)
    override fun setPrefixStream(stream: ByteArrayOutputStream) {
        this.prefixStream.reset()
        stream.writeTo(prefixStream)
    }

    @Throws(IOException::class)
    override fun close() {
        super.close()
        prefixStream.close()
    }

    override fun reset() {
        super.reset()
        prefixStream.reset()
    }

    override fun size(): Int {
        return super.size() + prefixStream.size()
    }

    override fun toByteArray(): ByteArray {
        val prefixBytes = prefixStream.toByteArray()
        val mainBytes = this.buf
        val combinedBytes = ByteArray(prefixStream.size() + super.size())
        System.arraycopy(prefixBytes, 0, combinedBytes, 0, prefixStream.size())
        System.arraycopy(mainBytes, 0, combinedBytes, prefixStream.size(), super.size())
        return combinedBytes
    }

    override fun toString(): String {
        return String(this.toByteArray())
    }

    @Throws(UnsupportedEncodingException::class)
    override fun toString(charsetName: String): String {
        return String(this.toByteArray(), charset(charsetName))
    }

    @Throws(IOException::class)
    override fun writeTo(out: OutputStream) {
        prefixStream.writeTo(out)
        super.writeTo(out)
    }
}